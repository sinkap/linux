# Kernel options required by the multinode test.
# Merge into a base config, e.g.:
#   scripts/kconfig/merge_config.sh -m .config \
#       tools/testing/multinode/multinode.config
#   make ARCH=<arch> olddefconfig
CONFIG_BPF_SYSCALL=y
CONFIG_BPF_JIT=y
CONFIG_BPF_EVENTS=y
CONFIG_DEBUG_INFO_BTF=y
CONFIG_DMA_SHARED_BUFFER=y
CONFIG_DMABUF_HEAPS=y
CONFIG_DMABUF_HEAPS_SYSTEM=y
CONFIG_DMABUF_HEAPS_CARVEOUT=y
CONFIG_BLK_DEV_INITRD=y
# BTF so the producer programs can be verified against kernel types;
# DEBUG_INFO_BTF depends on DEBUG_INFO.
CONFIG_DEBUG_INFO=y
CONFIG_DEBUG_INFO_DWARF_TOOLCHAIN_DEFAULT=y
CONFIG_DEBUG_INFO_BTF=y
